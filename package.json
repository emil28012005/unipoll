{
  "name": "unipoll-root",
  "version": "1.0.0",
"scripts": {
  "build": "npm install && npx vite build && cd server && npm install",
  "start": "node server/index.js"
},
  "dependencies": {
    "express": "^4.18.2"
  }
}