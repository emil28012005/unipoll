{
  "name": "unipoll-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && npm run build --prefix . && cd server && npm install",
    "start": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}